<VirtualHost *:80>
    DocumentRoot /.docker/api/php/public

    <Directory "/.docker/api/php/public">
        AllowOverride all
        Require all granted
    </Directory>

    ServerName api.costs-to-expect.local
    ErrorLog ${APACHE_LOG_DIR}/error.log
    CustomLog ${APACHE_LOG_DIR}/access.log combined
</VirtualHost>
